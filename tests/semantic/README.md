# semantic

## Purpose

Tests for deterministic Oracle → IR compilation and coverage metrics (M2).

## What belongs here

- Compiler/pattern regression tests over gold fixtures

## What does not belong here

- Full verifier gold_core suites (see `tests/gold_core/`)
