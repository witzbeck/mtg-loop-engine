# gold_core

## Purpose

Assert every curated `gold_core` positive verifies as `VERIFIED` with versioned proof metadata.

## What belongs here

- Pytest parametrize over `mtg_loop_engine.corpus.all_gold_core`

## What does not belong here

- Witness authoring (see `src/mtg_loop_engine/corpus/gold_core/`)
