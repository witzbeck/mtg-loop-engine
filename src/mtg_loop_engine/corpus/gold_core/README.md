# gold_core

## Purpose

Manually authored M1-must-pass two-card witnesses (IR + setup + loop body + `LoopRelevantState`) covering architectural shapes: mana/untap, tokens, ETB/death, zone recursion, counters, cost reduction, replacement interaction via negatives.

## What belongs here

- `cases.py` positives used by tests

## What does not belong here

- Spellbook dumps or blind search results
