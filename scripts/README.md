# scripts

## Purpose

Optional operator helpers. Primary CLI is `uv run mtg-loop-engine` (`mtg_loop_engine.cli`).

## What belongs here

- Ad-hoc maintenance scripts not imported as library code

## What does not belong here

- Package modules under `src/mtg_loop_engine/`
